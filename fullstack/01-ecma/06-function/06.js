// Definindo função ordinária
function sumMe(n1, n2) {
  // Expressões matemáticas devem ser isoladas entre (), ou o sinal de + concatenará variáveis
  return alert("sum = " + (n1 + n2))
}

// Invocar função
sumMe(1987, 32)