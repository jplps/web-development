//Definindo função literal identificada (nomeada)
var intSum = function sumInt(n1, n2) {
  return alert("sum = " + (n1 + n2))
}

//Utilizar função literal (note a ausência de parênteses)
alert(intSum)

//Chamado da variável que guarda a função
intSum("66", "6")